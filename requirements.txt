# ─────────────────────────────────────────────────────────────
# SmartRepo Analyzer — core requirements (required)
# المتطلبات الأساسية
# ─────────────────────────────────────────────────────────────
rich>=13.0            # terminal UI / واجهة الطرفية
pygments>=2.15        # language detection / كشف اللغات
pyyaml>=6.0           # YAML configs / ملفات الإعداد
toml>=0.10.2          # pyproject.toml parsing / قراءة pyproject

# ─────────────────────────────────────────────────────────────
# Recommended (better analysis)
# مستحسنة لتحليل أدق
# ─────────────────────────────────────────────────────────────
networkx>=3.0         # call-graph cycle detection / كشف حلقات النداء

# ─────────────────────────────────────────────────────────────
# Optional — install individually as needed
# اختيارية — ثبّتها حسب الحاجة
# ─────────────────────────────────────────────────────────────
# openai>=1.30       # --ai-key smart summarization / التلخيص الذكي
# radon              # --complexity metrics / مقاييس التعقيد
# bandit             # security scanning / الفحص الأمني
# pylint             # python linting / فحص جودة بايثون
# flake8             # style checking / فحص التنسيق
#
# GUI extras (also: tkinter ships with most Python builds):
# pillow>=10.0       # logo/image previews / معاينة الصور
#
# Node.js tools (npm): eslint, @mermaid-js/mermaid-cli
